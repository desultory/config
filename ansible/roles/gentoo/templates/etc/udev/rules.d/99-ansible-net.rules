# /etc/udev/rules.d/99-ansible-net.rules
# generated by an ansible script

{% for name, interface in netconfig.items() %}
{% if 'mac' in interface %}
SUBSYSTEM=="net", ACTION=="add", ATTR{address}=="{{interface['mac']}}", NAME="{{name}}"
{% endif %}
{% endfor %}
